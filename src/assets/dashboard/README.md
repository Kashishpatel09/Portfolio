Place dashboard screenshot images here (e.g. hospital-executive.png).

To use one in a project card, import it in `src/data/projects.js`:

  import hospitalImg from "../assets/dashboard/hospital-executive.png";

Then set `image: hospitalImg` on that project's object instead of `null`.
