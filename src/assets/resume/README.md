Don't put the resume PDF here — Vite would hash/bundle it, breaking the
plain download link. Put it in `public/resume/` instead (see the README
there). This folder is kept only to match the suggested project structure.
